{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Outer wrapper for Grep data.",
    "type": "object",
    "properties": {
        "logLines": {
            "$ref": "logLinesInner.json"
        }
    },
    "required": [
        "logLines"
    ]
}