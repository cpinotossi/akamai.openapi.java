{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Purge Request Body",
    "description": "Specifies a list of URLs to invalidate or delete.",
    "type": "object",
    "required": [ "objects" ],
    "properties": {
        "objects": {
            "type": "array",
            "items": {
                "description": "Lists URLs to purge. Purges content for both `http` and `https` schemes.",
                "type": "string"
            }
        }
    }
}
